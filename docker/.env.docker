# QueryMind - Docker Quick Start
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Override default model
OPENAI_MODEL=gpt-4o-mini

# Database credentials (auto-configured in compose)
DB_HOST=postgres
DB_USER=querymind
DB_PASSWORD=querymind123
DB_NAME=demo_db